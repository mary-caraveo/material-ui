import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#009688',
    },
    secondary: {
      main: '#0078AA',
    },
    tertiary: {
      main: '#705772'
    }
  },
});

export default theme;
